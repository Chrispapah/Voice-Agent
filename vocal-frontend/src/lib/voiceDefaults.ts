/** Default ElevenLabs voice for new agents and UI fallbacks (keep in sync with backend `SDRSettings.elevenlabs_voice_id`). */
export const DEFAULT_ELEVENLABS_VOICE_ID = "4hx4668A4ljDTKS4m5oV";

/** Greek STT: Deepgram uses `el`; Google STT on the server uses `el-GR` in `SDRSettings.google_speech_language_code`. */
export const DEFAULT_SPEECH_LANGUAGE_CODE = "el";
